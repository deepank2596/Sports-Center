version:    '3'

services:
    sports-center:
        build:  .
        command:    python manage.py runserver 0.0.0.0:8000
        volumes:
            -   .:/myapp
        ports:
            -   "8000:8000"